[preferred]
# Use the Hyprland portal when available, but fall back to GTK for portals it doesn't implement.
default=hyprland;gtk

# libadwaita/GTK4 apps (e.g. Nautilus) use the Settings portal for dark preference.
org.freedesktop.impl.portal.Settings=gtk

